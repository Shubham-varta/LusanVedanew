# Testing Rules

Before completing any feature:

Verify:

- Application builds
- No obvious TypeScript errors
- No obvious runtime errors
- Existing functionality still works

If packages are required:

Explain:

- Why
- What they install

Whenever a command is suggested:

Explain:

- What it does
- Why it is needed

Never assume the user understands the command.

Provide testing steps before stopping.
